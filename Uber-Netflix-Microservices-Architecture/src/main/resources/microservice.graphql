schema {
 query: Query
}

type Query {
 allData: [Microservices]
 single(id: ID): Microservices
}

type Microservices {
  id: ID
  microentity1: String
  microentity2: String
  microentity3: String
  microentity4: String
}

