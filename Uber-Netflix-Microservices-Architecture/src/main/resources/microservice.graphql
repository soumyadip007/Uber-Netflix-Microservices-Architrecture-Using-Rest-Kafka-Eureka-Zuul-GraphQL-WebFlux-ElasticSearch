schema {
 query: Query
}

type Query {
 allData: [GA4GH]
 single(id: ID): GA4GH
}

type GA4GH {
  id: ID
  microentity1: String
  microentity2: String
  microentity3: String
  microentity4: String
}

